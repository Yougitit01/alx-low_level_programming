#include <stdio.h>
/**
 * main - Entry point
 * Description: Numbers of base 10
 * Return; Always 0
 */
int  main(void)
{
	int n = 0;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
